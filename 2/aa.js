function differece(x) {
    let result = x - 10;

    // qualquer valor que seja atritbuido a resul sera convertido para um valor positivo
    return Math.abs(result)
}
differece()
console.log(differece(1))